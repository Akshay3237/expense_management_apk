import 'package:flutter/material.dart';

class CollaborationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Collaboration'),
      ),
      body: Center(
        child: Text('This is the Collaboration Page'),
      ),
    );
  }
}
